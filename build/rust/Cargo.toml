[package]
name = "cypherock-common"
version = "0.1.0"
edition = "2021"

# See more keys and their definitions at https://doc.rust-lang.org/cargo/reference/manifest.html

[dependencies]
prost = "0.11"
# Only necessary if using Protobuf well-known types:
prost-types = "0.11"
protobuf = "3.2.0"
hex = "0.4.3"

[build-dependencies]
prost-build = "0.11"
protobuf = "3.2.0"
